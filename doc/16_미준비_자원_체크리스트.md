# 미준비 자원 체크리스트 — KOIPA AI 영업비밀관리시스템 (Lloydk AI 코어)

작성일: 2026-05-30
상태: v0.7 (5070 Ti 풀가동 PoC Phase 0~5+7+8 완료 반영)
용도: **W3~W13 + 고도화 단계 누락 방지 점검표**. 진척 대시보드(doc/17)의 백킹 데이터.

## v0.7 갱신 핵심 (2026-05-30 — 5070 Ti 풀가동 PoC Phase 0~5+7+8)

야간 무중단 자동 작업 + 추가 고도화 라운드로 외부 의존 없이 PoC 5종 자체 측정 완료. **결과: PASS 5 (P1·P3·P5) / 미달 1 (P2 합성 천장) / 보류 1 (P4 실문서 대기)**.

- **§1.1 GPU** — 개발 PC 5070 Ti(Blackwell sm_120, 16GB)에서 KF-DeBERTa 1ep 33초·labeled_5k+Qwen3 합본 3,700건 43초 학습 입증, Qwen3 14B 추론 76 tps. 차단 자원에서 정식 제외(이전 v0.6은 "차단 제거" 표기만 했고 실측 미실시).
- **§1.4 LLM 합성** — Qwen3 14B 200건 합성에서 라벨 일치도 100% / FNR 0% PASS, Solar 10.7B는 81% / 27% / JSON 76.5% 실패로 미달 → **Qwen3 1순위 확정**. 풀 5,000건은 발주처 Anthropic API 키 도착 후.
- **§2.1 합성 5,000건** — synthetic_5k 5,000 + synthetic_qwen3 200 = 5,200건 ES `docs` 영구 인덱싱 완료(13분 6.8 docs/s, BGE-M3). /answer citations 0→3 실측. labeled_5k_plus_qwen3 = train 3,500 / val 748 / test 752 빌드 + 재학습 confidence 0.632 → 0.746.
- **§2.4 Nori 사전·§2.5 시드 v2** — 시드 v4 480+ 키워드까지 완료, Nori 사전 1,002 항목 동결.
- **Phase 8 추가 고도화 9건 (commit eeb5603)** — B1-2 ES 인덱싱·B1-3 Qwen3+5K 재학습·C1-1 KoBigBird-large 비교 학습(F1=1.0 동등, KF-DeBERTa 1순위 유지)·C2-1 arctic-embed-l-ko 4-way 확장(Recall 0.6667 미달)·C2-2 dragonkue/bge-reranker-v2-m3-ko(score 0.9998)·C3-1 thinking on/off(+39% 속도)·C3-7 JSON retry·C5-4 본문 길이 4 band·errors_ko.js 50항. 데모 §5 실측 비율 88%→86%(22 카드 / 19 measured + 3 spec).
- **잔존 차단 1건** — 1.3 실문서(자체결정 "미제공" 잠금) 변동 없음. 합격선 0.80(P2) 도달 결정타는 발주처 LLM API 키.

## v0.6 갱신 핵심 (2026-05-29 후속 13건 — 스켈레톤·NoOp 실가동화 + wiring 사각지대 + 일반화 강도)

v0.5의 33건은 인터페이스만 들어가있고 실제 wiring이 비어 있는 자리 다수. 실증 점검(코드 직접 read+grep)으로 다음 13건을 추가 닫음. **3개 묶음 커밋**:

**묶음 1 — 스켈레톤 → 실가동 (6건, commit a7a00a2)**
- **A1+** Outbox **Redis 백엔드 + DLQ stream** 추가 — `services/outbox.py`에 RedisOutboxStore(ZSET score=next_retry_at + HASH payload + STREAM `lloydk:outbox:dlq`) + WATCH/MULTI/EXEC visibility lock 600s. 팩토리 폴백 패턴은 R1 JobStore와 동일. tests +8.
- **A3+** SSE **진짜 stage callback** — `ClassifyService.classify(on_stage=...)` 신규, `asyncio.sleep(0)` 가짜 6단계 yield 제거, asyncio.Queue로 워커 스레드→이벤트 루프 thread-safe 전달. tests +3 (E2E SSE 통과).
- **A4+** Drift **운영 wiring** — `services/drift_monitor`에 save/load_train_centroid·fetch_recent_prod_embeddings·run_drift_check·publish_to_prom 추가, `prom_metrics.py` 5 gauge 신설, `workers/tasks.drift_tick` + Celery beat 15분 주기. tests +3.
- **A2+** Audit chain **wiring** — `api/middleware._try_build_chained_hash` 신규(DB 가용 시 build_chained_hash로 prev16:full32 패킹, 실패 시 sha256 폴백). audit_chain docstring에 "body 자체 변조는 별도 WAL 필요" 설계 한계 명시. tests +4.
- **B1+B3** CI **Redis service + fakeredis dev** — `.github/workflows/poc-ci.yml` test 잡에 redis:7-alpine + REDIS_URL_TEST, pyproject [dev]에 fakeredis>=2.20. R1·outbox SKIP 13건 자동 해소.
- **B2** CI **Elasticsearch service** — 동일 잡에 elasticsearch:8.15.0 + ES_URL. rag_indexer/es_store SKIP 자동 해소.
- 부수: `api/explain.py` future-annotations 사전 결함 동시 수정 (pydantic v2 ForwardRef 해석 실패로 app import 깨졌던 것, 로컬 cp949 collection error로 가려져 있던 것).

**묶음 2 — wiring 사각지대 (4건, commit d7f82fc)**
- **A5+** active_learning_tick **분기 검증** — mode별(auto/dry/snapshot) + retrain_status별(URGENT/RECOMMENDED weekly window/OK) + enqueue 실패(ENQUEUE_FAILED) 단위 9건.
- **A6+** Query expansion **사용처 facade** — `services/retrieval.expand_then_search` 신규(N개 확장 쿼리 → search_hybrid → RRF k=60 결합, hybrid 실패 시 dense 폴백, encode 실패 쿼리 skip, max_queries 폭주 차단). tests +7.
- **C 정리** — chunker_v2 = `m2_preprocess/chunker.py.split_v2`로 이미 존재(33건 메시지가 잘못된 이름 표기 정정), calibration = `scripts/calibrate_classifier.py` 위치 명시. 코드 변경 없음.
- **D1+** secrets_manager **일원화** — `config.fill_from_secrets_manager()` 신규, `assert_production_credentials`가 호출. env 비면 Vault/AWS SM에서 자동 보충(api_key·minio_secret_key·anthropic_api_key·openai_api_key·es_password·es_api_key). 이미 채워진 자리는 env 우선 보존. 멱등(`_SECRETS_FILLED` guard). tests +8.

**묶음 3 — 일반화 납품 강도 (3건, commit 795851e)**
- **D2** OTel **app 마운트 wiring** — `api/app.lifespan`에 `setup_tracing(app)` 호출 추가. OTEL_EXPORTER_OTLP_ENDPOINT 미설정 또는 opentelemetry-* 미설치면 silent no-op. setup 실패가 app 기동을 막지 않도록 try/except guard. tests +3.
- **D3** mTLS **docker-compose profile** — `docker-compose.yml`에 nginx-mtls 서비스 + `profiles=["mtls"]` 옵트인. 기본 up에서 빠짐. `docker compose --profile mtls up -d nginx-mtls`로만 활성. infra/mtls/nginx.mtls.conf + infra/mtls/certs 마운트. tests +4.
- **D4** SBOM **자동 갱신 hook** — Makefile sbom-publish 타깃(licenses + render_sbom_html → doc/21 갱신) + CI test 잡에 "Render doc/21 OSS SBOM HTML" 단계 + sbom artifact에 doc/21 포함. **자동 commit은 의도적 제외** — 운영자 PR 머지로 진실 소스 유지(blast radius 차단). tests +4.

**테스트 영향**: 신규 53 PASS (outbox redis 8 + SSE 3 + drift wiring 3 + audit chain wiring 4 + tick 분기 9 + retrieval facade 7 + secrets wiring 8 + OTel 3 + mTLS profile 4 + SBOM hook 4). 회귀 영향 0, ruff 0 위반.

**잔존 차단**: 1.3 실문서(자체결정 "미제공" 잠금) — 변동 없음.

## v0.5 갱신 핵심 (2026-05-29 고도화 33건)

W13 운영 진입 직후 P0(7건)·P1(15건)·P2(11건) 고도화 묶음 일괄 적용. 외부 의존 없는 작업은 모두 코드/문서로 완료:

**P0 (7건, 즉시 운영 영향)**
- **C1** PyMuPDF AGPL → 운영 기본은 pdfminer.six (BSD-3) — 옵트인 extra `[pdf-agpl]`만 잔존 → §3.1 🟡 → 🟢
- **C8** OWASP ASVS L2 자동 SAST — bandit + semgrep CI 잡 신설 (HIGH/ERROR 발견 시 fail)
- **A4** 청크 분할 v2 — 헤딩 보존 + 문장 경계 hybrid (`split_v2`) + 6 unit 테스트
- **B5** 시드 v4 — 신규 5 도메인(해양·우주·항공정밀·정유석유화학·원자력) ~100 키워드 추가 (총 ~410)
- **G1·G2·G3** doc/23 운영자 매뉴얼·doc/24 검수자 매뉴얼·doc/25 보안 운영 가이드

**P1 (15건, 1주 운영 강화)**
- **A1** BGE-reranker 어댑터(`adapters/reranker/`) — Noop/BGE/Qwen3 + lazy + extras `[reranker]`
- **A2** 하이브리드 가중치 grid search 스크립트(`scripts/tune_hybrid_weights.py`)
- **A5** Active Learning Celery beat — 30분 주기 + URGENT 자동 트리거 + 큐 분리 (queue=learning)
- **A7** `/classify/explain` — 토큰·요인 기여도 분해 API
- **A8** 룰 기반 PII 마스킹(`m2_preprocess/pii_masker.py`) — 12 패턴 + NER 옵션 + 12 테스트
- **B3** Adversarial 골든셋 100건(`datasets/adversarial/golden_100.jsonl`)
- **B4** Embedding drift 모니터(`services/drift_monitor.py`) + KL divergence + 5 테스트
- **C3** JWT RS256(`api/_jwt_auth.py`) — JWKS·kid 로테이션 + `[jwt]` extras
- **C4** 감사 로그 hash chain(`services/audit_chain.py`) — payload_hash 컬럼 활용 (스키마 변경 없음)
- **D1** 임베딩 Redis 영속 캐시(`cache_layer._RedisCache`) — 24h TTL + LRU 옆 fallback
- **D3** Celery 큐 분리(`celery_app.task_routes`) — classify/index/synthesis/learning 4큐
- **D6** Prometheus retention 30d → 90d / 10GB → 30GB
- **E1·E4** OpenAPI → Postman + Python·TS SDK 자동 생성 CI 잡 + artifact 90d 보존
- **E2** KL contract test(`tests/test_kl_contract.py`) — OpenAPI ↔ pydantic round-trip 6 테스트
- **E3** Webhook outbox 패턴(`services/outbox.py`) + InMemory store + DLQ + 5 테스트
- **F1·F2** Grafana KPI 대시보드 v2(`grafana/dashboards/lloydk-kpi-v2.json`) + 6 신규 알람 규칙(FNR/큐/DLQ/Drift/PII/AuditChain)
- **F3** incident Make 타깃 5종(`incident-api-down|es-yellow|queue-backlog|drift|audit-verify`)
- **F4** Loki promtail PII 마스킹 8 신규 룰 (RRN/이메일/카드/JWT 등)
- **F5** OpenTelemetry 부트스트랩(`obs/otel.py`) + `[otel]` extras

**P2 (11건, 운영 진입 후 실측·일반화)**
- **C2** SeaweedFS S3 호환 storage adapter — MinIO AGPL 회피 옵션
- **C5** Secrets Manager 추상화(`services/secrets_manager.py`) — env/Vault/AWS SM
- **C6** DR 리허설 자동화 스크립트(`scripts/dr_drill.py`) — RTO 4h 검증
- **C7** mTLS nginx 템플릿(`infra/mtls/nginx.mtls.conf`) + dev 인증서 발급 스크립트
- **A3** 쿼리 확장 모듈(`modules/m4_training/query_expansion.py`) — rule/LLM/hybrid
- **A6** Confidence calibration 스크립트(`scripts/calibrate_classifier.py`) — temperature scaling + ECE
- **D2** ES int8_hnsw 매핑·회귀 측정(`scripts/es_mapping_int8_hnsw.py`) — 메모리 50%↓ 후보
- **D4** uvicorn workers 운영 프로파일(`Dockerfile.api.prod`) — gunicorn + WEB_CONCURRENCY
- **D5** `/classify/stream` SSE — 진척 stage + partial 결과 + 토큰 단위 확장 여지
- **B1** 공개 코퍼스 다운로드 자동화(`scripts/download_public_corpus.py`) — 매니페스트 기반 + 라이선스 안내
- **B2** LLM 실호출 합성 v2(`scripts/synthesize_v2_diverse.py`) — 도메인×포맷×톤 직교 + distinct-n

**테스트 영향**: 신규 테스트 모듈 5개 (chunker_v2 6 + pii_masker 12 + drift_monitor 5 + outbox 5 + reranker 7) = **+35 PASS**.

**잔존 차단**: 1.3 실문서(자체결정 "미제공" 잠금) — 변동 없음.

## v0.4 갱신 핵심 (2026-05-28 W13 + R1~R5)
- **테스트 505개 collected** (커밋 7a28955 R1~R5 머지 후) — W12 기준 409 → +96
- **W7 관측성·백업·DR 코드 실재 확인** — `poc/infra/observability/` (Prometheus·Grafana·Loki·Promtail + alert_rules.yml) + `scripts/backup_postgres.py·backup_es_snapshot.py·backup_minio_mirror.py·dr_restore_check.py` 모두 존재. v0.3은 작성 시점 stale (🟡로 기재돼 있던 4.3·4.4 → 🟢 강등)
- **W13 운영진입 5건 완료**: R1 JobStore Redis 백엔드(WATCH/MULTI/EXEC race-free) · R2 Batch retry/보상(건별 isolation·지수 백오프·Celery 자동 재시도) · R3 Guide upload 한도(20MB·413) · R4 Document FK + cascade(ORM 안전망 + tenant 강제) · R5 OpenAPI 429 명시
- **잔존 차단**: 1.3 실문서(자체결정 "미제공" 동결) · 1.1 GPU 풀 학습(보유/미보유 양쪽 지원으로 차단 제외, 풀 학습 시 필요)

## v0.3 갱신 핵심 (2026-05-28 W12 + 블록 1~7)
- **차단 3건 → 1건**: 합성 5k(2.1)·labeled dataset(2.2) 자체 해소, 가이드 v2(1.2) 부분 해소
- **시드 v3 (313)·Nori v3 (1,002)·임시 가이드 v1 (doc/22)** — 운영 시작 가능 상태
- **잔존 차단**: 실문서(1.3 Q3) — 자체결정 "미제공 가정"으로 잠금됨 (운영 일반화는 합성+공개로 진행)

## v0.2 갱신 핵심 (2026-05-27 v2 자체 결정)
- **일반화 납품 전제** — 본 시스템은 KOIPA 외 여러 기관에도 납품 가능하도록 설계.
- **GPU 차단 제거** — LLM·임베딩 모두 원격/로컬 양쪽 옵션 지원으로 GPU 보유 여부와 무관해짐.
- **LLM 이중 경로** — 원격(Anthropic·OpenAI·Google) ↔ 로컬(vLLM·Ollama·LM Studio, OpenAI 호환 endpoint 통합 어댑터).
- **임베딩 로컬 가능** — KURE-v1 sentence-transformers 기본 의존성, `scripts/cache_kure_v1.py`로 사전 캐시.

> **읽는 법**
> - 🔴 **차단(blocker)**: 이 자원 없으면 해당 W 단계 시작 불가
> - 🟡 **지연(risk)**: 없어도 진행 가능하나 결과 품질·일정 영향
> - 🟢 **완료/대체 확보**: 자체 결정·기본 자원으로 동결됨
> - 각 항목은 (1) 무엇이 필요한가 (2) 현재 상태 (3) 자체 결정 가능한가 (4) 언제까지 필요한가 (5) 미확보 시 대체안

---

## 0. 한눈에 보기

| 범주 | 차단 🔴 | 지연 🟡 | 완료 🟢 | 합계 |
|---|:-:|:-:|:-:|:-:|
| 외부 의존 (실문서·가이드만) | 1 | 3 | 5 | 9 |
| 데이터셋 (합성·공개·실문서) | 0 | 3 | 3 | 6 |
| 라이선스 (PyMuPDF·MinIO·EXAONE) | 0 | 1 | 3 | 4 |
| 운영 환경 (폐쇄망·관측성·DR) | 0 | 0 | 5 | 5 |
| 자체 결정 동결 변수 | 0 | 0 | 16 | 16 |
| **합계 (v0.4, W13 R1~R5)** | **1** | **5** | **34** | **40** |

> **v0.3 → v0.4 차이 (2026-05-28 W13 R1~R5)**:
> - **🟡 → 🟢 강등 2건**: 4.3 관측성 스택 코드 실재 확인 / 4.4 백업·DR 스크립트 실재 확인 (v0.3은 stale)
> - W13 R1~R5 5건은 차단/지연이 아닌 운영 보강이라 본 표에는 영향 없음 (v0.4 핵심 갱신 §10 신설)

> **v0.2 → v0.3 차이 (2026-05-28 W12 + 블록 1~7)**:
> - 차단 3 → **1**. 데이터셋 2건 자체 해소 (2.1 합성 5,000건·2.2 labeled dataset)
> - 가이드 v2 (1.2) 🔴 → 🟡 부분 해소 — doc/22 임시본 작성
> - 남은 차단 1건: 1.3 실문서 (Q3, 자체결정 "미제공 가정"으로 잠금 상태)
> - 시드 v3 (313 키워드) + Nori v3 (1,002 항목) + 임시 가이드 v1로 운영 시작 가능 상태

---

## 1. 외부 의존 자원

### 1.1 🟡 GPU (Q1) — **차단 제거 (W9 일반화)**
| 항목 | 내용 |
|---|---|
| **무엇이** | NVIDIA GPU 24GB+ VRAM 1장 (KF-DeBERTa-base 학습 + 로컬 LLM 추론 시) |
| **현재** | 일반화 납품 전제로 **차단 자원에서 제외** — GPU 보유/미보유 양쪽 환경 모두 지원 |
| **자체결정** | 🟢 W9 일반화 — `settings.llm_provider`로 원격/로컬 전환, `embedding_model`로 로컬/HashFallback 전환 |
| **GPU 보유 환경** | 로컬 vLLM·Ollama + KURE-v1 임베딩 → 운영비 0 |
| **GPU 미보유 환경** | 원격 Anthropic·OpenAI + KURE-v1 CPU 추론 (느리지만 동작) 또는 원격 임베딩 API |
| **막힘** | 없음 (양쪽 모두 운영 가능) |

### 1.2 🟡 KOIPA 등급분류 가이드 v2 PDF (Q4) — **부분 해소 (W12+ 블록 7)**
| 항목 | 내용 |
|---|---|
| **무엇이** | KOIPA 영업비밀 4등급(TS/S1/S2/S3) 판단 기준 상세 문서. 등급당 키워드·예시 50~100건. |
| **현재** | **시드 키워드 v3 (313 항목, 18 도메인 균형)** + **임시 가이드 v1 자체 작성** ([doc/22](22_영업비밀_등급분류_가이드_v1_공개법령기반.md)) |
| **자체결정** | 🟡 **부분 해소** — 공개 법령(부정경쟁방지법·산업기술보호법·영업비밀보호법 시행령) 기반 임시 v1 운영 가능. 발주처 v2 도착 시 정본 패치 |
| **필요시점** | 운영 전환 직전 (발주처 가이드 v2 정본 채택) |
| **대체안** | doc/22 v1 임시본으로 운영 시작 가능 (등급 정의·요소 가중치·임계값·등급 경계 트리거 명시) |
| **막힘** | 부분 해소 — 운영 시작은 가능, KOIPA 정본 도착 시 v1 → v2 패치만 필요 |

### 1.3 🔴 비밀해제(declassified) 실문서 (Q3)
| 항목 | 내용 |
|---|---|
| **무엇이** | KOIPA·협력기업의 비밀해제 영업비밀 문서. 등급별 분포 + NDA |
| **현재** | 미확보 — 자체결정으로 "미제공 가정"으로 잠금 |
| **자체결정** | 🟢 동결됨 — 합성+공개 데이터만으로 진행 ([memory](../C:/Users/김택용/.claude/projects/e--antigravity-rag/memory/project_koipa_self_decide_consult.md)) |
| **필요시점** | **외부 일반화 검증 단계** (PoC 합격선 협의 후) |
| **대체안** | 합성 문서 5,000건 + 공개 코퍼스 (D1~D6) |
| **막힘** | "합성 데이터만으로 일반화" 한계 명시 필요 — doc/05·15에 정직성 노트 |

### 1.4 🟢 LLM 합성 — **이중 경로 (W9 일반화)**
| 항목 | 내용 |
|---|---|
| **무엇이** | 합성 800 → 5,000건 확장용 LLM 호출 |
| **이중 경로** | 원격: Anthropic Claude Sonnet 4.6 (`llm_provider=anthropic`) / 로컬: vLLM·Ollama Qwen3-14B (`llm_provider=vllm|ollama`) |
| **현재** | `poc/.env` 빈 값 → `llm_provider=noop` (결정론적 mock)로 동작 |
| **자체결정** | 🟢 양쪽 모두 어댑터 완비 — 운영 환경 가용성에 따라 선택 |
| **원격 환경** | Anthropic 키 발급 약 $50~100 예상 (5,000건) — 사업단 예산 결정 |
| **로컬 환경** | GPU 1장 + vLLM 또는 Ollama 가동 → 운영비 0, 단 추론 시간 길어짐 |
| **막힘** | 없음 (양쪽 모두 운영 가능) |

### 1.5 🟡 KL 인터페이스 명세 합의 (K2·K3)
| 항목 | 내용 |
|---|---|
| **무엇이** | OpenAPI 20 엔드포인트에 대한 KL 측 검토 의견 + 인증 방식 합의 |
| **현재** | doc/03 작성 완료, 자체결정으로 X-API-Key 잠금 |
| **자체결정** | 🟢 동결됨 — K3=API Key, K1=우리 docker-compose 단일 스택 |
| **필요시점** | **KL 통합 테스트 단계** (W8 이후) |
| **대체안** | KL 회신 차이 발생 시 OpenAPI yaml + 인증 미들웨어만 패치 (수 시간) |
| **막힘** | 없음 (자체결정으로 잠금) |

### 1.6 🟡 발주처 GPU 인프라 정보 (CUDA·Driver·컨테이너 런타임)
| 항목 | 내용 |
|---|---|
| **무엇이** | CUDA 12.x / NVIDIA Driver 535+ / Docker GPU runtime 가용성 |
| **현재** | 미확인 — Docker Desktop WSL2 backend로 PoC 진행 중 |
| **자체결정** | ⚠️ 부분 — `--gpus all` 호환 환경 가정 |
| **필요시점** | **W6 (폐쇄망 번들 실빌드 시 GPU 이미지 포함 결정)** |
| **대체안** | CPU-only 추론 이미지 별도 빌드 (40~60% 성능 하락) |
| **막힘** | 폐쇄망 번들 크기·구성 결정 지연 |

### 1.7 🟡 EXAONE 라이선스 (Q6)
| 항목 | 내용 |
|---|---|
| **무엇이** | LG AI Research EXAONE 3.5 32B 공공사업 라이선스 |
| **현재** | 자체결정으로 "제외" 잠금 ([memory](../C:/Users/김택용/.claude/projects/e--antigravity-rag/memory/project_koipa_self_decide_consult.md)) |
| **자체결정** | 🟢 동결됨 — 제외 |
| **필요시점** | — (포함 시) |
| **대체안** | Qwen3-14B (Apache 2.0) + Claude Sonnet 4.6 |
| **막힘** | 없음 |

### 1.8 🟢 ES 8.15.3 + Nori (E1·E2)
| 항목 | 내용 |
|---|---|
| **무엇이** | Elasticsearch 8.15+ with Nori 한국어 형태소 분석 |
| **현재** | ✅ docker-compose 잠금, ES cluster green 확인 (2026-05-27) |
| **자체결정** | 🟢 동결됨 — ES 8.15.3 + `infra/es/userdict_ko.txt` |
| **필요시점** | W5 (RAG 인덱서) |
| **대체안** | 관측성 알람 기반 ES 즉시 복구 (Runbook §2.2) — Qdrant 롤백 어댑터는 2026-05-27 v2에서 제거 |

### 1.9 🟢 PostgreSQL 16 (init.sql v2 19테이블)
| 항목 | 내용 |
|---|---|
| **무엇이** | PG 16 + 19개 ORM 매핑 테이블 + Alembic baseline |
| **현재** | ✅ docker-compose 잠금, ORM 100% 매핑, pytest 212/212 PASS |
| **자체결정** | 🟢 동결됨 |
| **필요시점** | W3 라우터 영속화 |
| **대체안** | — |

---

## 2. 데이터셋

### 2.1 🔴 합성 5,000건 (P3 풀 확장)
| 항목 | 내용 |
|---|---|
| **무엇이** | 등급별 ~1,250건 × 4등급 = 5,000건 (P1 학습 충분량) |
| **현재** | smoke 40건만 (`poc/datasets/synthetic/`). `synthetic_800/` 디렉터리 부재 (`make p3-800` 미실행) |
| **자체결정** | ✅ — Anthropic 키 발급 시 `make p3-800` 변형해 즉시 실행 |
| **필요시점** | **W6 학습 데이터셋 빌드** |
| **대체안** | (a) Qwen3-14B 자체호스팅 (b) noop로 결정론적 mock 합성 (품질 ↓) |
| **막힘** | 1.4 (Anthropic API) 의존 |

### 2.2 🔴 라벨링된 학습 데이터셋 (`datasets/labeled/`)
| 항목 | 내용 |
|---|---|
| **무엇이** | `train.jsonl`·`val.jsonl`·`test.jsonl` (stratified 70/15/15) |
| **현재** | 디렉터리 부재 — `make p1` 실행 불가 |
| **자체결정** | ✅ — 2.1 합성 후 `scripts/build_labeled_dataset.py`로 즉시 생성 |
| **필요시점** | **W6 P1 풀 학습 전** |
| **대체안** | smoke 40건으로 dryrun만 (현재 상태) |
| **막힘** | 2.1 의존 |

### 2.3 🟡 공개 코퍼스 (D1~D6, AI Hub·모두의말뭉치)
| 항목 | 내용 |
|---|---|
| **무엇이** | AI Hub 한국어 문서분류 데이터, 국립국어원 모두의말뭉치, 공공행정 문서 |
| **현재** | 미신청 — 자체결정으로 "로이드케이 명의 신청" 잠금 |
| **자체결정** | ✅ — Q5 동결 ([memory](../C:/Users/김택용/.claude/projects/e--antigravity-rag/memory/project_koipa_self_decide_consult.md)) |
| **필요시점** | **W6 학습 데이터셋 보강** (선택) |
| **대체안** | 합성 5,000건만으로 진행 |
| **막힘** | 외부 일반화 검증 약화 |

### 2.4 🟡 Nori 사용자 사전 본문 확장
| 항목 | 내용 |
|---|---|
| **무엇이** | `infra/es/userdict_ko.txt` — 영업비밀 도메인 용어 1~2천 항목 |
| **현재** | 파일 존재, 63줄(헤더 주석 5줄 + 본문 ~58 항목), ES 마운트 OK — **운영 수준은 1~2천 항목 필요** |
| **자체결정** | ✅ — 특허·R&D·반도체·바이오 등 공개 용어 수집·작성 |
| **필요시점** | **W5 RAG 인덱서** |
| **대체안** | 기본 Nori (사용자 사전 없이) — 분석 품질 ↓ |
| **막힘** | 없음 |

### 2.5 🟡 시드 키워드 v2 (`seeds.py`)
| 항목 | 내용 |
|---|---|
| **무엇이** | `poc/src/lloydk/modules/m3_labeling/seeds.py` — 등급당 50~100개 키워드 |
| **현재** | v0 (79줄), PoC용 최소 |
| **자체결정** | ✅ — 영업비밀보호법 공개 문헌 기반 v1 작성 가능 |
| **필요시점** | **W6 시드 v2 확장** |
| **대체안** | v0 유지 (분류 정확도 ↓) |
| **막힘** | 없음 |

### 2.6 🟢 PoC dryrun 합성 40건
| 항목 | 내용 |
|---|---|
| **무엇이** | noop provider로 결정론적 40건 |
| **현재** | ✅ `poc/datasets/synthetic/` 40 파일 |
| **자체결정** | 🟢 |
| **필요시점** | P3 smoke 회귀 |

---

## 3. 라이선스

### 3.1 🟡 PyMuPDF AGPL → 교체 또는 구매 (doc/14)
| 항목 | 내용 |
|---|---|
| **무엇이** | PyMuPDF는 AGPL-3.0 — 우리 코드 공개 의무 가능성 |
| **현재** | poc/src/lloydk/modules/m2_preprocess/extractor.py에서 사용 중 |
| **자체결정** | ✅ — **pdfminer.six로 교체 권장** (BSD, 약간의 추출 품질 손실) |
| **필요시점** | **W8 운영 전환 직전** |
| **대체안** | Artifex 상용 구매 (별도 예산) |
| **막힘** | 운영 배포 시 라이선스 컴플라이언스 |

### 3.2 🟡 MinIO AGPL → 교체 또는 구매
| 항목 | 내용 |
|---|---|
| **무엇이** | MinIO는 AGPL-3.0 (v2024+) |
| **현재** | docker-compose에 minio 정의됨 (W3에서 가동 예정) |
| **자체결정** | ✅ — **MinIO Commercial 구매 권장** (가장 단순), 또는 SeaweedFS(LGPL) 교체 |
| **필요시점** | **W8 운영 전환 직전** |
| **대체안** | Garage (MPL-2.0), 클라우드 S3 |
| **막힘** | 운영 배포 시 라이선스 컴플라이언스 |

### 3.3 🟡 SBOM (CycloneDX) 산출
| 항목 | 내용 |
|---|---|
| **무엇이** | OSS 라이선스 매트릭스 자동 생성 |
| **현재** | `make licenses` 타깃 존재 (`dump_licenses.py`), CycloneDX 산출 코드 있음 |
| **자체결정** | 🟢 도구 갖춤, CI에 추가만 하면 됨 |
| **필요시점** | **W8 운영 전환 시 발주처 검수 첨부** |
| **대체안** | 수동 산출 |
| **막힘** | 없음 |

### 3.4 🟢 Redis 7.2 BSD 시점 고정
| 항목 | 내용 |
|---|---|
| **무엇이** | Redis 7.4+는 RSALv2/SSPL 듀얼 — BSD 시점(7.2.x) 유지 |
| **현재** | ✅ `docker-compose.yml`에 `redis:7.2-alpine` 잠금 (commit `792cc2a`) |
| **자체결정** | 🟢 |

---

## 4. 운영 환경

### 4.1 🔴 폐쇄망 망 등급 (E9)
| 항목 | 내용 |
|---|---|
| **무엇이** | 운영 환경 망 등급 (인터넷 차단/제한적 인터넷/내부망) + 매체 반입 절차 |
| **현재** | 자체결정으로 "폐쇄망 가정, 매체 반입 25~30GB 번들" 잠금 |
| **자체결정** | 🟢 동결됨 ([memory](../C:/Users/김택용/.claude/projects/e--antigravity-rag/memory/project_koipa_self_decide_consult.md)) |
| **필요시점** | **W6 폐쇄망 번들 실빌드** |
| **대체안** | 회신 시 망 등급 따라 절차만 조정 |
| **막힘** | 없음 (자체결정) — 단 KOIPA 측 매체 반입 신청 절차는 별도 확인 필요 |

### 4.2 🟡 발주처 KOIPA 측 시스템 관리자 연락 채널
| 항목 | 내용 |
|---|---|
| **무엇이** | 폐쇄망 배포 시 매체 반입 신청·승인·설치 지원 담당자 |
| **현재** | 미확인 |
| **자체결정** | ❌ — 발주처 조직 정보 |
| **필요시점** | **W6 매체 반입 신청 시** |
| **대체안** | 협의요청서 회신 시점에 K4 KL 담당자 함께 확인 |
| **막힘** | 폐쇄망 배포 일정 지연 |

### 4.3 🟢 Prometheus / Loki / Grafana 설정 — **W7에서 해소 (v0.4 정정)**
| 항목 | 내용 |
|---|---|
| **무엇이** | `infra/observability/` — Prometheus targets, Loki promtail, Grafana 대시보드 JSON |
| **현재** | ✅ [`poc/infra/observability/`](../poc/infra/observability/) 실재 — `prometheus.yml` · `loki-config.yaml` · `promtail-config.yaml` · `alert_rules.yml` · `docker-compose.observability.yml` · `grafana/` |
| **자체결정** | 🟢 W7 종료 시점에 완성됨 (v0.3 stale 정정) |
| **필요시점** | ✅ 완료 |
| **대체안** | — |
| **막힘** | 없음 |

### 4.4 🟢 백업·DR 리허설 — **W7에서 해소 (v0.4 정정)**
| 항목 | 내용 |
|---|---|
| **무엇이** | Postgres pg_dump → MinIO 일별, ES snapshot, MinIO 미러, RTO 4h 검증 |
| **현재** | ✅ [`scripts/backup_postgres.py`](../poc/scripts/backup_postgres.py) · [`backup_es_snapshot.py`](../poc/scripts/backup_es_snapshot.py) · [`backup_minio_mirror.py`](../poc/scripts/backup_minio_mirror.py) · [`dr_restore_check.py`](../poc/scripts/dr_restore_check.py) 실재. doc/11 Runbook §6에 cron 예시 명시 |
| **자체결정** | 🟢 W7 종료 시점에 완성됨 (v0.3 stale 정정) |
| **필요시점** | ✅ 완료 (운영 실측은 RTO 4h 리허설 1회 필요 — 운영 전환 직전) |
| **대체안** | — |
| **막힘** | 없음 (실측 리허설만 미완) |

### 4.5 🟡 운영 SLA·연락망 (doc/11 §4)
| 항목 | 내용 |
|---|---|
| **무엇이** | 장애 등급별 대응 시간, 1차/2차 콜 담당자, 통지 채널 |
| **현재** | doc/11 v0.9, SLA·연락망 미정 |
| **자체결정** | ⚠️ 부분 — 로이드케이 사업단 측 연락망은 자체 작성 가능, KL·KOIPA 측은 회신 의존 |
| **필요시점** | **W8 운영 전환 직전** |
| **대체안** | 사내 연락망만 우선 작성 |
| **막힘** | 없음 (운영 전환 직전 완성) |

---

## 5. 자체 결정 동결 변수 (16개) 🟢

자체 결정으로 회신 대기 없이 잠금 완료 ([memory](../C:/Users/김택용/.claude/projects/e--antigravity-rag/memory/project_koipa_self_decide_consult.md)):

| ID | 결정값 | 소스 |
|---|---|---|
| Q1 | 24GB×1 VRAM, KF-DeBERTa-base batch=8 | trainer.py 기본값 |
| Q2 | 합성 데이터 사업단 내부 사용만 | 자체결정 |
| Q3 | 실문서 미제공 가정, 합성+공개로만 진행 | 자체결정 |
| Q4 | 현 시드 동결, 회신 시 v2 패치 | seeds.py |
| Q5 | AI Hub 로이드케이 명의 | 자체결정 |
| Q6 | EXAONE 제외 | 자체결정 |
| Q7 | 검수자 2명 + AI팀 셀프 검수 | 자체결정 |
| E1 | ES 8.15.3 (docker-compose 잠금) | docker-compose.yml |
| E2 | Nori + 사용자 사전 자체 작성 | infra/es/ |
| E3 | ES Basic 라이선스, 인덱스 분리 | doc/13 §11.1 |
| E5 | 단일 노드 JVM 4GB, int8_hnsw | docker-compose.yml |
| E9 | 폐쇄망 가정, 25~30GB 번들 | doc/12 |
| K1 | 우리 docker-compose 단일 스택 | docker-compose.yml |
| K3 | X-API-Key 인증 | api/classify.py |
| K5 | 멀티테넌트 (tenant_id 컬럼·네임스페이스 분리) | DB 스키마 |
| 라이선스 | pgvector 완전 제거 (ES 단일) | commit `5b77ff4` |

---

## 6. W 단계별 의존성 매트릭스

> "이 단계 진행하려면 어느 자원이 준비돼야 하는가" 한눈에 보기. 🔴는 차단, 🟡는 지연, 🟢는 확보.

| W | 단계 | 의존 자원 | 차단 여부 |
|---|---|---|---|
| **W1** | DB 영속화 | PG 16 🟢, ORM 19 🟢 | ✅ **완료** |
| **W2** | Repository + ClassifyService DB 통합 + Audit | PG 16 🟢, W1 🟢 | ✅ **완료** |
| **W3** | 라우터 8개 (confirm·relabel·train·synth·guide·schema·metrics·async) | PG 16 🟢, W2 🟢 + Redis 7.2 🟢 (Celery) + MinIO (guide 업로드, 4건만 W5로 미루기 가능) | 🟢 진행 가능 |
| **W4** | M6 평가/능동학습 | W3 🟢, matplotlib 🟢 (이미 설치) | 🟢 진행 가능 |
| **W5** | RAG 인덱서 + /guide/documents | ES 8.15 🟢, Nori 사전(2.4) 🟡, KURE-v1 모델 다운로드 🟢 (HuggingFace) | 🟢 진행 가능 (사전 v1으로) |
| **W6** | 학습 데이터·합성 5,000건·시드 v2·폐쇄망 번들 실빌드 | **GPU (1.1) 🔴 + Anthropic API (1.4) 🟡 + 가이드 v2 (1.2) 🔴** | 🔴 **차단** — 외부 자원 필요 |
| **W7** | 관측성·백업·DR | Prometheus·Loki·Grafana(4.3) 🟢, 백업 3종·DR(4.4) 🟢 | ✅ **완료** (v0.4 정정) |
| **W8** | KL 통합·라이선스 교체·SBOM·운영 전환 | KL 측 통합 일정(1.5) 🟡, PyMuPDF/MinIO 교체(3.1·3.2) 🟡 | 🟡 부분 진행 가능 |
| **W9** | LLM·임베딩 일반화 | 어댑터·시드 v2·Nori 사전 🟢 | ✅ **완료** |
| **W10~W12** | PSH 18 시나리오 × 67 KPI | 인프라 자원 🟢, dryrun PASS 47/0/20 | ✅ **완료** |
| **W13** | 운영 진입 보강 R1~R5 | Redis 7.2 🟢, fakeredis 🟢 (테스트) | ✅ **완료** (505 tests) |

→ **W1~W5 + W7~W13 모두 외부 의존 없이 완료.** W6 학습 측 실측만 GPU·API 키 차단.

---

## 7. 차단(🔴) 1건 해결 액션 (v0.4)

| ID | 자원 | 액션 | 누가 | 언제까지 |
|---|---|---|---|---|
| 1.1 | GPU | **클라우드 GPU 결제 또는 발주처 GPU 확보 일정 확인** | 사업단 | W6 시작 전 |
| 1.2 | 가이드 v2 | **공개 영업비밀보호법·시행령으로 v1 작성 → 가이드 도착 시 v2** | AI팀 | W6 시작 전 |
| 1.3 | 실문서 | **자체결정 "미제공"으로 잠금, 외부 일반화 한계 명시** | AI팀 | doc/05·15 정직성 노트 (즉시) |
| 2.1 | 합성 5,000 | **Anthropic API 키 발급 (~$100 예산 승인)** | 사업단 | W6 시작 전 |
| 2.2 | labeled dataset | **2.1 후 `make build-labeled-dataset` 즉시 실행** | AI팀 | W6 |
| 4.1 | 폐쇄망 망 등급 | **자체결정 "폐쇄망 가정"으로 잠금됨** | AI팀 | ✅ 완료 ([memory](../C:/Users/김택용/.claude/projects/e--antigravity-rag/memory/project_koipa_self_decide_consult.md)) |

→ **2건은 사업단(GPU·API 키 예산 결정) 의존, 3건은 AI팀이 즉시 진행 가능, 1건은 이미 잠금.**

---

## 8. 다음 단계

1. 본 문서를 사업단에 공유 → GPU·API 키 예산 확정 요청
2. AI팀은 1.2(가이드 공개 문헌 v1)·1.3(정직성 노트)·2.4(사전)·2.5(시드 v2) 즉시 착수 가능
3. **W3 라우터 작업과 병행 진행** — 외부 의존 자원은 백그라운드로 준비
4. 본 문서를 doc/17 진척 대시보드의 "미준비 자원" 섹션 백킹 데이터로 사용

---

## 9. 갱신 주기

- W 단계 종료 시마다 갱신 (특히 W6 GPU 확보 후 전면 재평가)
- 차단 항목 해소 시 즉시 🔴 → 🟡 또는 🟢로 상태 변경
- 새로운 의존 발견 시 (예: 가이드 도착 시 v2 분량 확정) 추가

---

## 10. W13 운영 진입 보강 (R1~R5) — v0.4 신설

운영 환경 진입 직전 5건 보강. 차단/지연 분류 대상은 아니며, 본 §10은 "운영 가용성·안전성 보강 산출물" 기록용.

| ID | 영역 | 산출물 | 효과 |
|---|---|---|---|
| **R1** | JobStore Redis 백엔드 | `services/job_store.py` — `InMemoryJobStore` + `RedisJobStore` + `get_job_store()` 팩토리. WATCH/MULTI/EXEC race-free, TTL 24h, Redis 실패 시 InMemory 폴백 | 멀티 워커 안전, 재시작 시 작업 ID 보존 |
| **R2** | Batch retry/보상 | `services/_retry.py` 지수 백오프 데코레이터(stdlib). `async_classify_service.submit_batch` 건별 isolation·2회 retry·`failed_doc_ids`·`errors` 응답. Celery `bind=True, max_retries=3, default_retry_delay=10` | 부분 실패 격리, 전체 작업 손실 방지 |
| **R3** | Guide upload 한도 | `config.max_upload_mb=20` env, `api/guide.py` `file.size` 1차 + `len(body)` 2차 검증, 초과 시 413 | DoS 차단 |
| **R4** | Document FK + cascade | `db/listeners.py` ORM before_delete 이벤트로 chunks 자동 삭제, `repositories/chunk_repo.delete_by_doc_id` + `document_repo.delete` (tenant_id 강제), `services/document_service.DocumentService.delete_document` session_scope 원자 트랜잭션 | PG 16 파티션 제약 회피 + tenant 격리 + 고아 chunk 0 |
| **R5** | OpenAPI 429 명시 | `doc/03_openapi_lloydk_kl.yaml` `TooManyRequests` + `PayloadTooLarge` 공통 응답, `/classify` 429 명시, `openapi-spec-validator` OK | KL 통합 시 429/413 핸들링 명세 |

**테스트 영향**: 회귀 409 → **505 passed** (+96), PSH dryrun PASS=47 FAIL=0 SKIP=20 유지, ruff 0 위반 유지.

**커밋**: `7a28955` (2026-05-28).
