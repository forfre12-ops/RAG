# 실문서 S3 후보 v-a6e21b73 — 게이트 FAIL. 채택하지 않는다

작성 2026-08-16. 채택 기준은 `REAL_S3_ADOPTION_CRITERIA_2026-08-16.md` 에 **학습 전에**
고정해 두었다. 그 기준으로 판정한 결과다.

---

## 1. 결과

```
axis1  hardened42    f1 0.8094 vs 0.9166 | 고→S3 5 vs 0 | S1 재현율 0.643 vs 0.857   FAIL
axis2  공개문서 FPR   과분류 0.0125 vs 0.0400                                          PASS
axis3  적대 100건     고→S3 7 vs 1 | 하향 31 vs 39 (sev 44 vs 49)                      FAIL
OVERALL: FAIL
```

`reports/v5_gate/gate_realS3.json`

---

## 2. 의도한 효과는 실제로 나왔다

```
공개문서 과분류율   0.0400 -> 0.0125    69% 감소
```

**서식을 학습에 넣으니 공개 문서를 영업비밀이라 부르는 비율이 크게 줄었다.** 가설은 맞았다.

---

## 3. 그런데 대가가 안전이었다

```
axis1  고등급이 S3(비밀 아님)로 샌 것   0건 -> 5건
axis3  같은 항목                       1건 -> 7건
S1 재현율                              0.857 -> 0.643
f1_macro                               0.9166 -> 0.8094
```

**비밀 문서를 "비밀 아님" 으로 놓치는 건수가 늘었다.** 우리가 절대 허용하지 않기로 한
방향이고, 헛경보 감소로 상쇄할 수 있는 종류가 아니다.

---

## 4. 예상했던 위험이 현실이 됐다

데이터를 만들 때 이렇게 적어 두었다.

> ⚠ 상한을 600 으로 잡았다. S3 를 과하게 넣으면 모델이 S3 로 쏠려 **미탐이 는다** -
> 그것이 우리가 가장 피해야 할 방향이다.

가중치를 0.5 로 낮춰 S3 신호 비중을 43.4% 로 맞췄는데도 **부족했다.**

한 가지 눈에 띄는 것이 있다.

```
under_graded          39 -> 31   줄었다
under_graded_severity 49 -> 44   줄었다
high_risk_to_s3        1 ->  7   늘었다
```

**전체 하향은 줄었는데 하향의 성격이 나빠졌다.** 한 단계씩 내리던 것이 S3 까지 내려가는
쪽으로 바뀌었다. `under_graded` 만 봤으면 개선으로 읽었을 값이다 - `high_risk_to_s3` 를
별도로 두는 이유가 여기 있다.

---

## 5. 봉인 400건은 쓰지 않았다

채택 기준 §1 이 "안전 - 하나라도 어기면 탈락" 이다. **안전에서 탈락했으므로 효과를 재는
것은 의미가 없다.** 봉인셋은 손대지 않고 남긴다(sha256 `e04d07fa850d401231ac40e0...`).
다음 후보에 그대로 쓴다.

---

## 6. 다음에 시도한다면

이 데이터가 쓸모없다는 뜻이 아니다. **넣는 방식이 틀렸다.** 시도할 수 있는 것:

```
가중치를 더 낮춘다        0.25 · 0.1
건수를 줄인다             200 · 400
S3 만 넣지 말고 균형      TS·S1 을 같이 늘려야 하는데 실문서가 없다  <- 이게 근본 제약
등급별 손실 가중 조정      S3 쪽 가중을 낮춰 학습
```

⚠ 다만 **탐색을 반복하면 봉인셋이 오염된다.** work 절반으로만 탐색하고, 봉인 400건은
  최종 후보 하나에만 쓴다. 여러 후보를 봉인셋으로 고르면 그것이 곧 봉인셋에 맞추는 것이다.

⚠ 그리고 근본 제약은 그대로다 - **S3 만 실문서로 늘리면 균형이 깨진다.** TS·S1 실문서가
  없으면 이 방향은 한계가 있다.

---

## 7. 확인된 사실 정리

```
서식 학습 투입은 헛경보를 실제로 줄인다      0.0400 -> 0.0125 (실측)
그 대가로 위험 미탐이 는다                  0 -> 5 · 1 -> 7 (실측)
가중치 0.5(S3 신호 43.4%)로는 부족했다
현행 배포본 v-fe4b386b 를 유지한다
```
