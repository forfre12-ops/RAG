# 182 서버 배포 완료 — 검수 착수 준비까지

작성 2026-08-15. 지재원·고객사 이중 스택에 오늘 작업분을 배포하고 검수 화면을 띄웠다.

---

## 1. 배포 결과

```
서버        182.212.163.182:56320 (aisadm)
스택        lloydk-jjw(지재원 :8000) · lloydk-cust(고객사 :8001)
커밋        14f2d6498356
검증        healthz build.git_sha = 14f2d6498356  <- 종전에는 확인 방법 자체가 없었다
상태        jjw 200 · cust 200
```

배포된 오늘 작업분이 컨테이너 안에서 실제로 도는 것까지 확인했다.

```
legacy_office.extract_ppt_text        존재
management_from_metadata('none','all_employees') -> ('proven_absent', 0)
_source_prior_is_public('public')     -> True
```

---

## 2. 배포 중 걸린 것 — 리네임이 남긴 세 군데

`lloydk -> koipa` 리네임(2026-08-12)이 서버 쪽에 세 개의 잔재를 남겼고, 셋 다
**배포를 실제로 실패시켰다.** 223 서버에서도 같은 순서로 터진다.

```
1. uv.lock           서버가 8/9(리네임 전) 락파일이라 빌드가
                     "Missing workspace member koipa-ai" 로 죽었다
                     -> 락파일 동기화

2. 외부 볼륨          compose 가 koipa_hf_cache 를 요구하는데 서버엔 lloydk_hf_cache
                     -> koipa_hf_cache 생성 + 2.8GB 모델 캐시 복사(재다운로드 회피)

3. 환경변수 이름       코드가 KOIPA_AUDIT_CHAIN_SECRET 을 읽는데 .env 는 LLOYDK_ 접두
                     -> API 가 startup 에서 죽었다(production 자격증명 누락)
                     -> KOIPA_ 이름을 추가(기존 LLOYDK_ 은 되돌림용으로 보존)
```

⚠ 3번을 만났을 때 **서비스가 내려간 상태**라 먼저 롤백해 복구한 뒤 원인을 고쳤다.
   롤백 태그 `:rollback-20260815` 6개가 그때 쓰였고 지금도 남아 있다.

---

## 3. 검수 착수 상태

> **정정(2026-08-20).** 이 절의 등급 분포가 `TS 31 · S1 42 · S2 42 · S3 79` 로 적혀
> 있었다. **합이 194 로 120 건과 맞지 않는다.** 같은 파일을 다시 세어 보니 네 등급이
> 각 30 건이었다(`label` 기준). `rule_grade`(S3 30·S1 5·None 85)·`llm_grade`
> (S3 18·S2 11·S1 6·None 85)로도 그 수가 나오지 않는다. **옛 수치가 어디서 나온
> 것인지는 확인하지 못했다** — 다른 데이터셋의 값일 가능성이 있다.
> 확인 명령: `python -c "import json,collections;
> print(collections.Counter(json.loads(l)['label'] for l in
> open('poc/datasets/golden_review/ff5a822c/candidates.jsonl',encoding='utf-8')))"`


```
전달본      datasets/golden_review/ff5a822c/candidates.jsonl  120건
            등급 TS 30 · S1 30 · S2 30 · S3 30 (파일 실측 2026-08-20)
마운트      /home/aisadm/poc/datasets -> /app/datasets  (API·워커 공유)
서명 잡      5a36202a-f214-4c38-b086-3c031718f378  status=done gold_count=120
서명 화면    /api/v1/golden/jobs/<job>/signoff.html?t=<token>   200 · 736KB
API 역할     API_KEY_ROLE=admin  <- 등록·서명 둘 다 가능(교집합)
```

### 검수자에게 줄 것

API 가 `127.0.0.1` 바인드라(리버스 프록시 뒤 전제) **SSH 터널이 필요하다.**

```
ssh -i <키> -p 56320 -L 8000:127.0.0.1:8000 aisadm@182.212.163.182

브라우저:  (job-id·token 은 서명 잡 등록 결과에서 얻는다 -
           scripts/register_review_signoff_job.py 출력 또는 GET /api/v1/golden/jobs)
  http://127.0.0.1:8000/api/v1/golden/jobs/<job-id>/signoff.html?t=<token>
```

### 반드시 지켜야 할 것

```
1. 서명 계정은 **실계정** 이어야 한다
   ai_assist · demo-console · system · codex 는 서비스가 거부한다
   (거부되면 응답에 rejected_reasons={'machine_reviewer': N})

2. 서명 요청에 **publish=true**
   기본 false 는 미리보기라 정본·라이브 readiness 를 안 건드린다

3. 등급별 5건(총 20건)이면 locked_eval 구조적 게이트가 열린다
   ⚠ 그것이 GA 조건은 아니다 — readiness 의 P1 classifier 게이트는 별도다
```

⚠ **JobStore 가 in-memory 면 API 재시작에 잡이 사라진다.** 그때는 등록을 다시 하면
   된다(`scripts/register_review_signoff_job.py` 는 멱등이다).

---

## 4. 223(KL) 서버는 아직이다

```
SSH      키 미등록 · 비밀번호 대화형 필요 · 182 경유도 차단(SSH·API 둘 다 closed)
```

접근 경로가 없어 내가 배포하지 못한다. 다만 **소스는 GitHub 에 있고 서버가 거기 닿는지만
확인하면 된다** — 182 는 닿았다.

```
ssh kopia@223.130.156.134
cd ~/poc
git init -q 2>/dev/null; git remote add origin https://github.com/forfre12-ops/RAG.git
git fetch --depth 1 origin fix/design-review-hardening
git archive FETCH_HEAD poc/src poc/scripts poc/tests poc/Dockerfile.api.prod poc/Dockerfile.worker \
  poc/uv.lock poc/pyproject.toml poc/docker-compose.yml poc/docker-compose.prod.yml \
  | tar -x --strip-components=1 -C .

# ⚠ 위 §2 의 세 잔재를 **먼저** 확인할 것
grep -c KOIPA_AUDIT_CHAIN_SECRET .env*        # 0 이면 추가해야 한다
docker volume ls | grep koipa_hf_cache        # 없으면 생성 + 복사
grep -c koipa uv.lock                         # 0 이면 락파일이 옛것이다

docker compose build --build-arg KOIPA_BUILD_SHA=$(git rev-parse FETCH_HEAD) \
                     --build-arg KOIPA_BUILD_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)
docker compose up -d --no-build
curl -s localhost:8000/api/v1/healthz | python3 -c "import json,sys;print(json.load(sys.stdin)['build'])"
```

마지막 줄이 배포한 커밋을 찍으면 반영 확인이다.

---

## 5. 롤백

```
docker tag <이미지>:rollback-20260815 <이미지>:latest    # 6개
ENV_FILE=.env.jjw  API_HOST_PORT=8000 docker compose -p lloydk-jjw  ... up -d --no-build
ENV_FILE=.env.cust API_HOST_PORT=8001 docker compose -p lloydk-cust ... up -d --no-build
```

백업: `~/poc_src_backup_20260815.tgz` · `~/uv.lock.bak_20260815` · `~/.env.*.bak_20260815`

---

## 6. 2차 배포 `ab780792b2f5` — ICD 메타데이터 경로

```
:8000 lloydk-jjw  (full-train)    ok · git_sha ab780792b2f5 · v-fe4b386b
:8001 lloydk-cust (onprem-local)  ok · git_sha ab780792b2f5 · v-fe4b386b
```

배포본에서 메타데이터가 실제로 판정을 바꾸는지 같은 본문으로 확인했다(:8000).

```
메타 없음                    S1  needs_review
access_scope=approved_only  S1  needs_review  metadata-management
access_scope=all_employees  S1  needs_review  metadata-management-conflict  <- M=0 인데 S1 → 검수
security_marking=secret     S1  needs_review  metadata-management
source_type=public          S3  needs_review  source-prior · cap-conflict   <- ICD enum 수정 라이브 확인
```

⚠ 로컬(룰 폴백)에서는 같은 본문이 S3 였다. **배포본은 실모델이라 S1 을 낸다** — 로컬
   결과로 배포본 동작을 판단하면 안 된다(기록된 교훈: 로컬 venv 로 배포본 진단 금지).

### 배포 중 걸린 것 — 프로젝트 이름

`deploy_testserver_dual.sh` 헤더는 `koipa-jjw`/`koipa-cust` 인데 **실제 서버에 도는
스택은 `lloydk-jjw`/`lloydk-cust`** 다(리네임 전 이름 그대로). 내가 헤더대로 새 프로젝트로
올려서 컨테이너·볼륨 세트가 중복 생성됐고, 포트 충돌로 API 만 안 떴다.

```
증상   Bind for 127.0.0.1:8000 failed: port is already allocated
원인   기존 lloydk-jjw-api-1 이 그 포트를 정상 점유 중
```

⚠ 이때 `docker ps | grep koipa` 로 확인해서 **기존 스택이 안 보였고** 서비스가 내려간
   줄 알았다. 실제로는 둘 다 healthy 였다. 필터가 만든 오진이다.

조치: 중복 `koipa-*` 스택을 `down -v` 로 제거(방금 생성된 빈 볼륨이라 데이터 없음),
이미지 6개 태그를 `lloydk-*` 로 찍고 기존 프로젝트에 `up -d --no-build`.

```
⚠ 빌드는 기본 프로젝트명으로 돌아 koipa-poc-{api,worker,beat} 를 만든다.
   배포 전 반드시 lloydk-{jjw,cust}-{api,worker,beat}:latest 로 태그해야 한다 - 6개다.
```

롤백 태그를 새로 남겼다(직전 8/15 1차 빌드).

```
lloydk-{jjw,cust}-{api,worker,beat}:rollback-20260815b   <- 이번 배포 직전 = 1차 빌드
lloydk-{jjw,cust}-{api,worker,beat}:rollback-20260815    <- 그 이전(리네임 전)
```

---

## 7. 223 배포 준비 — 스크립트를 182 에서 검증했다

### 223 의 현재 상태(SSH 없이 확정)

`223:8000` 이 외부로 응답하므로 **OpenAPI 로 배포본을 직접 진단할 수 있다.** 접속
권한 없이 확인한 결과다.

```
uptime            220,744s = 2.6일    → 마지막 재기동 8/13
build 필드        없음                 → B-1 미배포
/documents        ICD 필드 없음         → B-2b 미배포
/documents/analyze ICD 필드 없음        → B-2b 미배포
/golden/candidates review_batch 없음    → B-3 미배포
deploy_profile    full-train           → metadata_floor_enabled 는 True (프로파일이 켠다)
```

**8/14~8/15 작업이 하나도 없다.** KL 시험이 223 에서 돌면 오늘 고친 것이 전부 빠진
빌드를 시험하게 된다.

⚠ 진단 순서를 기록해 둔다 — `healthz` → `openapi.json` 의 paths·스키마. 404 해석을
   먼저 하면 접두사 오판을 반복한다(기록된 교훈).

### 배포 스크립트

`scripts/deploy_kl_223.sh` 를 서버에서 한 번 실행하면 끝난다. §2 의 지뢰 넷을 모두
자동 탐지한다(이름 하드코딩 없음 — 223 의 실제 이름을 모르기 때문이다).

**182 에서 전 구간 실행해 검증했다.**

```
프로젝트 탐지   lloydk-cust · lloydk-jjw       (라벨에서 되읽음)
env 탐지        .env.cust · .env.jjw           이미 정합
볼륨            koipa_hf_cache 있음
빌드            koipa-poc-{api,worker,beat}    <- name: koipa-poc 가 디렉터리명을 이긴다
태그            :rollback-202608151227 보존 후 :latest 6개
검증            git_sha 436cb838bd38 두 스택 일치 · security_marking OpenAPI 노출 2회
```

⚠ 작성 중 한 번 틀렸다. 빌드 산출 이미지명을 `basename $PWD` 로 추정했는데
  `docker-compose.yml` 1행의 `name: koipa-poc` 이 디렉터리명을 이긴다(디렉터리는 `poc`).
  그대로 뒀으면 6단계에서 이미지를 못 찾고 죽었다.

### 막고 있는 것 — SSH 접근 하나

```
223 :22    OPEN (도달됨. 이전 "차단" 기록은 틀렸다)
인증       공개키 미등록 · 비밀번호 인증만 받음
```

비밀번호 자동 입력은 정책상 막혀 있다. 공개키를 등록하면 그 뒤로는 전부 자동이다.

```
# 223 에서 한 번만 실행
mkdir -p ~/.ssh && chmod 700 ~/.ssh
echo 'ssh-ed25519 AAAA... claude-code@lloydk-deploy' >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys

# 지문 확인용
SHA256:LRbpVnJDveZtNZiNanyn+VMyCg4bWHpkRTiu1bXCm54
```

등록되면 배포는 한 줄이다.

```
ssh -i ~/.ssh/kipai_testserver kopia@223.130.156.134 'bash ~/poc/scripts/deploy_kl_223.sh'
```
