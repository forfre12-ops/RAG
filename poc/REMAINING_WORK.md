# 남은 일 — 실행 백로그

갱신 2026-08-16. **이 문서는 백로그다.** 완료 이력이 아니다. 이력은 git log 에 있다.

> **규칙: 항목마다 근거(파일·명령·실측값)를 함께 적는다. 근거를 못 대면 백로그에 넣지 않는다.**
> 확인하지 않은 것은 "확인 안 함"이라고 적는다. 추측을 사실처럼 적지 않는다.

분류는 넷이다.

```
A. 우리가 지금 할 수 있는 것
B. 다른 쪽이 움직여야 하는 것
C. 사람이 정해야 하는 것
D. 지금은 못 고치는 것 (기록만)
```

---

## 배포 대상 두 곳 — 성격이 다르다

2026-08-15 사용자 확정 · `docs/DEPLOY_GUIDE.md` 24·47행.

```
지재원    연결망 · GitHub 접근 가능   -> 소스 받아 빌드·기동 (182 와 같은 절차)
고객사    폐쇄망(에어갭)              -> 오프라인 설치 파일 묶음(약 5.9GB) 필요
```

⚠ 지재원에 오프라인 설치 묶음은 **필요 없다.** 내가 한 번 반대로 말했다가 정정받았다.

---

## A. 우리가 지금 할 수 있는 것

| ID | 항목 | 확인된 상태 | 근거 |
|---|---|---|---|
| ~~A-1~~ | 번들에 라이선스·SBOM 넣기 | ✅ 2026-08-16 완료 | 번들 `licenses/` 6개 파일 |
| ~~A-2~~ | 재학습 전 과정 1회 실행 확인 | ✅ 2026-08-16 KL 서버에서 완주 (70분 · v-24c7c02c 생성·DB 등록). 원인은 워커 메모리 4GB 상한 SIGKILL 순환이었다 | 완료 |
| ~~A-5~~ | 실문서 S3 600건 학습 후보 판정 | ❌ 2026-08-16 **두 번 FAIL**(가중치 0.5·0.25). 헛경보는 0.0400→0.0125~0.0150 로 줄지만 axis3 위험미탐이 1→7 로 늘고 **가중치를 절반으로 낮춰도 안 줄었다**. 가중치가 원인이 아니라 등급 구성이다. 배포본 유지 (`REAL_S3_GATE_FAIL_2026-08-16.md` · `REAL_S3_W025_GATE_FAIL_2026-08-16.md`) | 종결 |
| **A-3** | 저장 암호화 키 주입 | 미착수 | 하드닝 프로파일은 `storage_encryption_enabled=True`, 키는 별도 |
| **A-4** | JWT 발급자·대상·JWKS 설정 | 미착수 | 현재 `auth_mode=api_key` |

### A-2 상세 — 재학습

과거 학습 작업 11건을 열어 확인했다(`GET /train/jobs`).

```
완료 2 · 실행중 2(8/8 제출, 8일째 멈춤) · 실패 7
완료 2건도 job 기록의 model_version 이 null
```

**다만 모델 파일 자체는 생성됐다.** `artifacts_out/classifier/` 에 8/8 산출물 3개가 있다.

```
v-4e069693 · v-55eee599 · v-ada1c942
각각 config.json · model.safetensors · temperature.json · report.json 보유
```

즉 **학습은 되는데 job 기록에 모델 버전이 연결되지 않는다.** 등록 단계가 미완이다.

8/8 실패 원인 4종 중 3종은 코드에서 이미 해소됐다(오늘 확인).

```
datasets/labeled/val.jsonl 없음   -> 기본값이 datasets/labeled_p1_v5_clean 로 변경됨(config.py:641)
                                     컨테이너에 실재 확인
artifacts/classifier 읽기 전용     -> 출력이 artifacts_out/classifier 로 변경됨(config.py:649)
                                     컨테이너 마운트 rw=true 확인
sample_weight 범위 거부           -> 상한 제거(trainer.py:319, weight <= 0.0 만 거부)
SoftTimeLimitExceeded             -> **확인 안 됨**. celery_task_soft_time_limit=900 인데
                                     8/8 완료 작업은 38분·68분 걸렸다. 설정과 맞지 않는다
```

⚠ 코드는 고쳐졌는데 **그 뒤로 아무도 실행해보지 않았다.** 2026-08-16 에 실행 중
  (`train_job_id=60c493ad-ffe4-4250-93a6-31aa59a460f9`).

---

## B. 다른 쪽이 움직여야 하는 것

| ID | 항목 | 무엇이 막고 있나 | 근거 |
|---|---|---|---|
| ~~B-1~~ | 223 서버(KL) 배포 | ✅ 2026-08-16 배포·검증 완료(91c79f05980e · 검수 화면 외부 200 · 워커 16GiB) | 완료 |
| **B-2** | 골든셋 후보 검수 | 0건 | 아래 상세 |
| **B-3** | 실제 보안등급 문서 | 없음 | 학습용 S1/TS 200~400건 |
| **B-4** | EDMS 가 메타데이터 3필드 전송 | 미협의 | 배관은 완성·배포됨 |
| **B-5** | 메타데이터 오류율 5% 합의 | 미협의 | 20%면 사전등록 조건 붕괴 |

### B-1 — 223 서버 현재 상태 (SSH 없이 확인)

`223.130.156.134:8000` 이 외부로 응답하므로 OpenAPI 로 배포본을 직접 진단했다.

```
uptime 2.6일          마지막 재기동 8/13
build 필드 없음        빌드 sha 노출 미배포
/documents            ICD 필드 없음
/documents/analyze    ICD 필드 없음
/golden/candidates    review_batch 없음
```

**8/14~8/15 작업이 하나도 반영돼 있지 않다.**

배포 절차는 `scripts/deploy_kl_223.sh` 로 준비돼 있고 182 에서 전 구간 실행 검증했다.
리네임(2026-08-12) 잔재 5종을 자동 탐지한다.

```
1) uv.lock 이 리네임 전        2) 외부 볼륨 이름 불일치      3) .env 접두 LLOYDK_
4) compose 프로젝트명           5) compose 파일 자체가 옛 이름(airgap compose 13곳)
```

### B-2 — 검수가 릴리스 게이트를 막고 있다

```
[release-gate] BLOCKED: human review gold  0/40
```

```
20건(등급별 5)   locked_gold_eval 구조 게이트 개방
40건            정식 릴리스 게이트 개방 -> GA 등급 번들 생성 가능
189건           "무음 미탐 2% 이하" 주장 가능 (Wilson 0/189 = 0.0199)
```

⚠ 189건 계산은 **그 표본이 실문서일 때만** 실문서에 대한 주장이 된다. 합성이면 합성에
  대한 주장이다.

검수 화면은 살아 있다 — `signoff.html` HTTP 200 · 120건 · 재배포 2회 후에도 유지.

---

## C. 사람이 정해야 하는 것

| ID | 항목 | 왜 결정이 필요한가 |
|---|---|---|
| **C-1** | 고객사 배포 일정 | **확인한 바 없음.** 언제 어디로 보낼지 못 정한다 |
| **C-2** | 5.9GB 설치 파일 전달 방법 | 고객사는 폐쇄망이라 물리 매체 필요. 절차를 모른다 |

---

## D. 지금은 못 고치는 것 (기록만)

| ID | 항목 | 확인된 사실 |
|---|---|---|
| **D-1** | 무음 미탐 2건 | S1 을 S2 로 낮게 보고 자동확정. 둘 다 155~200자. S1/S2 경계는 비밀관리성으로 갈리는데 본문에 없다 -> B-4 가 오면 풀릴 가능성 |
| **D-2** | 룰 엔진 S·V 미탐지 | 2026-08-12 에 시드 보강·의미매칭·임계탐색 3종을 다 시도해 전부 실패(`RULE_EXTRACTOR_DIAGNOSIS`). 8/15 에 **표기만** 정정했다(역산값을 '룰 근거'라 부르던 것) |
| **D-3** | S1 재현율 0.87 | 실제 S1 문서가 없어 못 올린다. B-3 가 유일한 경로 |

---

## 오늘까지 확인해서 닫은 것

```
빌드 sha 노출 안 됨          배포·확인 완료 (healthz.build.git_sha)
ICD enum 인식 안 됨          배포·라이브 확인 (source_type=public -> S3)
ICD 메타 3경로 중 2곳 끊김    수정·배포·라이브 확인
검수 서명 작업 미등록          등록됨. 재배포 2회에도 생존 (HTTP 200)
beat·redis 실환경 미확인      beat 60초 주기 발화 · 워커 6시간에 28건 처리
임베더 무음 대체 위험          폴백 기록 0건 · 실모델 로드 확인
번들에 6/2 모델 적재          게이트 4개를 차단형으로 전환 · 번들 분류기 = 배포본 동일본
P1 게이트 판정 단위           원시 모델 -> 서빙 경로 (사용자 승인 · 두 숫자 병기)
번들 라이선스 미동봉          6개 파일 동봉 완료
```
