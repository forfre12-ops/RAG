# ⚠ 이 폴더의 파일로 업로드 정확도를 재지 마십시오

`docx/` · `pdf/` 의 72건은 **본문 첫 줄에 정답이 적혀 있습니다.**

```
문서번호: G50-TS-04 보안등급: [특급기밀]
문서번호: LK-DOC   보안등급: [2급대외비]
```

실측 2026-08-22(`scripts/check_demo_docs.py --dir datasets/gold/formats/docx --measure`):

```
72건 중 자동확정 46건 (64%) · conf 0.77~0.976
```

이 수치는 **분류 성능이 아니라 머리말을 읽은 결과**입니다. 같은 계열의 실업무문서 형태
(`poc/demo_formats` · `poc/demo_samples`, 29건)에서는 자동확정이 5건(17%)입니다.

## 원본 데이터셋은 깨끗합니다

등급 표기는 **docx/pdf 로 만들 때 생성기가 붙인 머리말**이고, 원본 JSONL 에는 없습니다.

| 원본 | 등급 표기 포함 |
|---|---|
| `datasets/gold/golden50.jsonl` | 0 / 52 (0.0%) |
| `datasets/gold/golden100_labeled_v2.jsonl` | 0 / 100 (0.0%) |
| `datasets/gold/golden500.jsonl` | 0 / 500 (0.0%) |
| `datasets/gold/classification_gold.jsonl` | 11 / 144 (7.6%) |

즉 **학습·평가 데이터 오염이 아니라 파일 생성 단계의 문제**입니다.

## 쓸 수 있는 곳 / 없는 곳

| 용도 | 가능 |
|---|---|
| 파서·추출 경로 시험(포맷별 텍스트 추출) | ○ |
| 업로드 E2E 배선 확인 | ○ |
| **분류 정확도·자동확정률 측정** | **✕** |
| **시연** | **✕** — 발주처가 본문을 열면 등급이 보입니다 |

## 남은 조치

머리말 없이 재생성하거나, 측정용으로 쓸 때는 첫 줄을 제거하고 넣어야 합니다.
생성기가 저장소에 없어 이번에는 경고만 남깁니다(2026-08-22).
