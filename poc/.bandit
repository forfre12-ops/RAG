[bandit]
# C8: 보안 SAST — bandit 설정
# 운영 진입 직전 OWASP ASVS L2 스캔.
exclude = tests,migrations,scripts/legacy,build,dist,.venv
skips = B101,B404,B603,B607
# B101 assert_used — pytest assert는 정상
# B404 subprocess import — backup·DR 스크립트에서 의도적 사용
# B603/B607 subprocess call without shell — 신뢰 가능 인자에만 사용
