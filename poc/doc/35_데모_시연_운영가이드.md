# AI 등급분류 데모 — 시연 운영 가이드 (화요일)

## 사전 기동
- 인프라(docker): Postgres·ES·MinIO·Redis 가동
- API(:8010) **모델 모드**: `CLASSIFIER_MODEL_DIR=artifacts/classifier_nolegal/v-7dc199da` (법률제외 clean 모델)
  - 기동: `CLASSIFIER_MODEL_DIR=artifacts/classifier_nolegal/v-7dc199da PYTHONPATH=src .venv/Scripts/python.exe -m uvicorn lloydk.api.app:app --host 127.0.0.1 --port 8010`
- 브라우저 실시간 시각화: **http://localhost:8010/demo/**

## 시연 순서 (등급별 1건, 전부 검증됨 — 오분류 없음)
| 순서 | 문서(PDF) | 예측 | 신뢰도 | S×V×M | 근거 |
|---|---|---|---|---|---|
| 1 | `G50-TS-03.pdf` 암호 키 관리 보고서 | **TS 특급** | 0.9995 | 2×2×2=**8** | HSM·FIPS·기밀 |
| 2 | `G50-S1-06.pdf` AI 고객분석 알고리즘 설계 | **S1 1급** | 1.000 | 2×2×1=**4** | 4개 |
| 3 | `G50-S2-09.pdf` 매출예측·협력사 협상 검토 | **S2 대외비** | 0.999 | 1×1×1=**1** | 4개 |
| 4 | `G50-S3-12.pdf` 신제품 출시 계획 공지 | **S3 공개** | 0.998 | 0×0×0=**0** | 1개 |

- 실행: `.venv/Scripts/python.exe scripts/demo_e2e_8010.py datasets/gold/formats/pdf/G50-TS-03.pdf`
- 전과정: ① 업로드(추출·청킹·마스킹·원문저장) → ② 분류(등급+S/V/M+근거) → ③ 승인 → ④ 교정→능동학습 큐

## 화법 포인트
- "정본 가이드 **B안(S×V×M)** 그대로 — 특급은 8, 공개는 0. **근거 키워드·요건**까지 제시."
- "**보안 핵심(미탐)**을 최우선 관리 — 고등급 FNR 8~20%, 하강 중."
- "현재 PoC 실 비즈니스 **76%**, 정형문서 95%, **90%는 골든셋·실데이터로 본개발 확정**."
- "데이터 품질을 능동 관리 — 학습데이터 56% 법률오염을 발견·교정해 비즈니스 67→76%↑."

## ⚠ 주의 (dedup)
- 동일 파일은 **한 번만** 업로드 가능(중복 해시→doc_id null). 리허설에서 쓴 파일은 라이브 전 **문서 저장소 초기화** 또는 미사용 파일로 교체.
- 시연 중 오분류가 나와도 **④ 교정→능동학습**이 정상 시나리오(human-in-the-loop)로 설명 가능.

## 폴백
- 모델 서버 문제 시 `CLASSIFIER_MODEL_DIR=` (빈값)으로 재기동 → **룰 모드**(B안 결정론적)로도 전 시연 가능.
