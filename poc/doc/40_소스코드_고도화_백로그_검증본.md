# 소스코드 고도화 백로그 (검증본)

- **작성일:** 2026-06-20
- **대상:** `poc/src/lloydk` (146 파일, ~23k LOC) + 인프라(docker-compose·Makefile·alembic·scripts·static)
- **방법:** 서브시스템 6분할 병렬 감사 → 상위 항목 적대적(refute-first) 재검증 → 직접 코드/레퍼런스 대조
- **범위 주의:** 내부 엔지니어링 백로그. `open/` 공유본 트리밍 정책(원가·협상·내부정보 제거) 대상 아님 — KL/고객 공유 금지.

## 범례
- ✅ **이번 브랜치(`fix/design-review-hardening`)에서 수정 완료**
- 🔧 미착수(권고)
- 판정: CONFIRMED(실재) / NUANCED(부분사실·영향제한) / 오탐정정

---

## 🔴 Tier 0 — 배포 전 필수 (운영 차단급)

| # | 항목 | 판정 | 상태 |
|---|---|---|---|
| 1 | Celery 워커가 `-Q` 없이 기동 → 라우팅된 작업 미소비 (docker-compose.yml:142) | CONFIRMED | ✅ worker `-Q classify,index,synthesis,learning,celery` 추가 |
| 2 | `celery beat` 서비스 부재 → drift·auto-rollback·outbox·active-learning tick 미발화 | CONFIRMED | ✅ compose에 `beat` 서비스 + `make beat` 추가 |
| 3 | 데드 라우트 `lloydk.index_documents` (태스크 미정의) | CONFIRMED | ✅ task_routes에서 제거 |
| 4 | audit hash chain read-then-write race (middleware.py:107 + audit_chain.py:201) — 동시요청이 같은 prev를 읽어 체인 분기, verify_chain 통과 | CONFIRMED (완화책 없음) | ✅ `build_chained_hash_locked`로 prev-read+insert를 advisory-lock 단일 tx에 결속 |
| 5 | 파티션 자동 생성 부재 — audit_log는 2026-07부터 default 파티션行 (baseline_init.py:178) | CONFIRMED | ✅ 백필 마이그레이션 `e5f6a7b8c9d0` + `ensure_partitions_tick`(beat 매일) |
| 40 | 폐쇄망 오프라인 번들이 학습 가중치·`temperature.json` 누락 → 미학습 베이스+무보정 서빙 (build_offline_bundle.py) | CONFIRMED | ✅ `--classifier-model-dir`로 학습 가중치+temperature.json 동봉 |

> Tier 0의 #1~3은 "납품했는데 폐곡선 자동화가 실제로는 안 도는" 묶음. #40은 폐쇄망 납품 핵심 시나리오 결손. #4·#5는 컴플라이언스·운영 지속성 직결이라 구현 규모가 커 별도 작업.

---

## 🟠 Tier 1 — 보안·분류 신뢰성

| # | 항목 | 판정 | 상태 |
|---|---|---|---|
| 6 | explain/stream tenant 결속 누락 (IDOR) — explain.py:92·classify_stream.py | CONFIRMED | ✅ `resolve_effective_tenant` 결속 추가 |
| 7 | Idempotency-Key 전역 네임스페이스 → 교차테넌트 응답 replay (_idempotency_mw.py:32) | CONFIRMED | ✅ 자격증명+테넌트 헤더 해시로 키 prefix |
| 12 | API 키 비-상수시간 비교 (_jwt_auth.py:418) | CONFIRMED (low) | ✅ `hmac.compare_digest` 적용 |
| 13 | JSON batch body 크기 파싱 후 검사 (async_classify.py:39) | CONFIRMED | 🔧 ASGI max-body 미들웨어 |
| 8 | LLM 라벨러 confidence 영구 0.0 (llm_labeler.py:62 — 프롬프트 스키마 누락) | CONFIRMED | ✅ 프롬프트 confidence 출력 + SVM 결정성 폴백(`_coerce_confidence`) |
| 10 | 캘리브레이션 런타임 측정 부재 (ECE/Brier) — pipeline.py 적용만 | CONFIRMED | ✅ `m6/calibration.py`(ECE/Brier/reliability + DB산출) + report 섹션 + calibrate 더미 가드 |
| 11 | reviewer_trust recency 편향 — latest correction=합의+self-agreement (reviewer_trust.py:57) | CONFIRMED | 🔧 동료 합의 기반 + min_corrections 상향 |
| 41 | calibrate_classifier 출력이 서빙 미참조 경로(`artifacts/classifier/`)에 기록 | CONFIRMED | ✅ `--output` 기본값을 `<model-dir>/temperature.json`로 유도 |

### ⚠️ 1차 분석 정정 (재검증으로 하향)
- **#9 serving_eval fail-secure FNR 편향:** fail-secure(`pred="TS"`) 메커니즘은 serving_eval.py:61에 실재하나, **deploy_gate는 이 함수를 쓰지 않음**(trainer의 `TrainReport.test_fnr_high` 사용). "회귀 모델이 게이트 통과"는 성립 안 함 → **진단 리포트 정확도(Med)로 강등**. 호출처는 `scripts/eval_serving_path.py`·테스트뿐.

---

## 🟡 Tier 2 — 검색·성능 품질

| # | 항목 | 판정 | 상태 |
|---|---|---|---|
| 14 | 이중 RRF (es_store search_hybrid가 RRF 융합 → retrieval.py:206이 재RRF) | **NUANCED** (기본 reranker 재정렬·단일쿼리 순위보존으로 실효 영향 제한) | 🔧 단일 result_set 시 원점수 보존 |
| 15 | 운영 권장 `split_v2` 미사용 (indexer.py:97 v1 split) → 조항/문장 절단, heading_path 유실 | CONFIRMED | 🔧 split_v2 전환 |
| 16 | LLM 답변 인용 검증 경고만 (answer.py:225) → 환각 인용 그대로 반환, grounding 런타임 미호출 | CONFIRMED | 🔧 심각 시 deterministic 강등 옵션 |
| 17 | 재인덱싱 고아 청크 (document_indexer.py:79 단일 collection upsert) | CONFIRMED | 🔧 선삭제 또는 content_hash 멱등 |
| 18 | LLM provider 재시도 비대칭 (Anthropic만 백오프) | CONFIRMED | 🔧 base로 재시도 추출 |
| 19 | ES bulk 부분실패 침묵 (`_errors` 폐기) | CONFIRMED | 🔧 retry_on_timeout + 실패건 로깅 |
| 20 | 임베딩 dim 하드코딩(1024) — KURE-v1 차원 불일치 위험 (hf_embedding.py:22) | CONFIRMED | 🔧 실측 dim |
| 21 | PRICE_TABLE: opus-4-7 $15/$75(실제 $5/$25, 3배), opus-4-8 누락→cost=0 | CONFIRMED | ✅ 단가 정정 + opus-4-8/4-6 추가 |
| 22 | 룰엔진 semantic 매칭 N회 재임베딩 (rule_engine.py:251) | CONFIRMED | 🔧 문서 임베딩 1회 재사용 |
| 23 | rule-fallback이 severe-agg/τ 우회 (m5_inference pipeline.py:407) | CONFIRMED | 🔧 폴백도 max-pooling+τ 공유 |
| 24 | 임베딩 캐시 디스크 플러시 O(N) + 락 없는 순회 (cache_layer.py:182) | CONFIRMED | 🔧 주기/종료 플러시 |
| 25 | corrections 재구축 train↔holdout 누수 미차단 (corrections_rebuild.py:156) | CONFIRMED | 🔧 holdout 교집합 제외 enforce |
| 42 | 프론트엔드 stored-XSS — admin.html이 title·근거 text를 escape 없이 innerHTML 주입 | CONFIRMED | ✅ admin.html `esc` 헬퍼 + app.js `escapeHtml` 적용 |

---

## 🟢 Tier 3 — 견고성·운영성

| # | 항목 | 판정 | 상태 |
|---|---|---|---|
| 26 | guide UNIQUE 제약 부재 (guide_repo.py:34 select-then-insert, 락 없음) | CONFIRMED | ✅ `(guide_id,version,tenant_id)` UNIQUE 마이그레이션 추가 |
| 26b | corrections UNIQUE 부재 | **NUANCED** (confirm_service의 `with_for_update` 행 잠금으로 이미 직렬화) | 🔧 Low — 최종 방어선용 부분 UNIQUE |
| 27 | N+1/풀스캔 (mark_corrections_consumed, add_correction) | CONFIRMED | 🔧 bulk UPDATE |
| 28 | LLMUsageService가 공유 풀 우회 (별도 engine, llm_usage_repo 데드코드) | CONFIRMED | 🔧 공유 SessionLocal 통합 |
| 29 | 비즈니스 OTel span 0개 (otel.py:55 자동계측만) | CONFIRMED | 🔧 핵심 경로 수동 span |
| 30 | 구조화 로깅 부재 + Celery trace 전파 누락 | CONFIRMED | 🔧 JSON 포매터 + CeleryInstrumentor |
| 31 | config 검증 로직 전무 (field_validator 0개) | CONFIRMED | 🔧 soft<hard, ratio 0~1, Literal |
| 32 | SSE 스트림 disconnect 미감지 + rate-limit 미적용 (classify_stream.py) | CONFIRMED | 🔧 협조적 취소 + @limiter.limit |
| 33 | 순수 단위테스트가 fullstack 오마킹 (test_perf_regression.py:17) | CONFIRMED | 🔧 마커 제거 |
| 34 | 워커 tick 단위테스트 빈 구역 (auto_rollback/deliver_outbox/train) | CONFIRMED | 🔧 tick 단위테스트 |
| 35 | outbox InMemory attempts 락 밖 변형 (outbox.py:90) | CONFIRMED | 🔧 dequeue 시 deepcopy |
| 36 | storage Protocol에 list/delete 부재 (storage/base.py:6) | CONFIRMED | 🔧 인터페이스 보강 |
| 37 | 멀티테넌트 인덱스명 충돌 (_safe_token 50자 절단, indexer.py:218) | CONFIRMED | 🔧 절단 시 해시 suffix |
| 38 | soft-delete/보존정책 부재 (document_service.py:111 hard delete) | CONFIRMED | 🔧 deleted_at + 보존기간 |
| 39 | drift 임계 단일 고정값(KL≥0.5), 연속위반·검색 연계 없음 (drift_monitor.py:36) | CONFIRMED | 🔧 히스테리시스 + recall 트리거 |

> alembic↔models 정합은 재검증 결과 **깨끗함**(이슈 없음).

---

## 이번 브랜치에서 수정한 9건 요약
1. explain/stream tenant 결속 (IDOR) — `api/explain.py`, `api/classify_stream.py`
2. Idempotency 키 주체 네임스페이스 — `api/_idempotency_mw.py`
3. API 키 상수시간 비교 — `api/_jwt_auth.py`
4. PRICE_TABLE 단가 정정 + opus-4-8 — `adapters/llm/base.py`
5. guide UNIQUE 제약 — `db/models.py` + `alembic/versions/d4e5f6a7b8c9_*.py`
6. worker `-Q` 전체 큐 구독 — `docker-compose.yml`, `Makefile`
7. `beat` 서비스 신설 — `docker-compose.yml`, `Makefile`
8. 데드 라우트 제거 — `workers/celery_app.py`
9. (문서) 본 백로그

## 2차 구현 완료 (이번 세션 — Tier 0/1 잔여 7건)
1. #4 audit chain advisory lock — `audit_chain.build_chained_hash_locked` + `middleware._raw_body_hash`/`_record`
2. #5 파티션 롤오버 — `services/partitions.py` + `tasks.ensure_partitions_tick` + beat + 마이그레이션 `e5f6a7b8c9d0`
3. #40 폐쇄망 번들 학습모델 동봉 — `scripts/build_offline_bundle.py`(`--classifier-model-dir`)
4. #8 LLM 라벨러 confidence — `m3_labeling/llm_labeler.py`
5. #10 ECE 런타임 측정 — `m6_evaluation/calibration.py` + `report.py` + calibrate 더미 가드
6. #41 calibrate 출력경로 — `scripts/calibrate_classifier.py`
7. #42 프론트엔드 XSS — `api/static/admin.html`, `api/static/app.js`

## 3차 일괄 구현 완료 (이번 세션 — Tier 2/3 잔여 전부)
배치별 파일분할 병렬구현 → 배치별 통합테스트 → 커밋. 모든 동작변경은 보수적(opt-in/no-op/FNR-safe).
- **Batch1 견고성(422d730)**: #13 body가드 · #17 고아청크(vectorstore delete) · #18 provider 재시도 · #19 ES bulk · #20 임베딩 dim 실측 · #22 룰 재임베딩 1회화 · #24 캐시 플러시 · #33 fullstack 오마킹
- **Batch2 검색품질(1e2491a)**: #15 split_v2(heading_path) · #16 인용 집행(opt-in 강등) · #23 rule-fallback τ/severe-agg · #25 holdout 누수차단 · #14 이중 RRF
- **Batch3 운영성(81f94a6)**: #29 OTel span · #30 구조화로깅+Celery trace · #31 config 검증 · #39 drift 히스테리시스
- **Batch4 데이터/DB/API(606036f)**: #27 N+1 · #28 공유풀 · #32 SSE disconnect+rate-limit · #36 storage list/delete · #37 인덱스명 충돌 · #38 soft-delete · #26b corrections UNIQUE (마이그레이션 f6a7b8c9d0e1)
- **Batch5(7425ed7)**: #35 outbox 원자성 · #34 워커 tick 단위테스트 11건
- **#11 reviewer_trust(47da347)**: self-reference 편향 제거(동료 합의 기반). 정책 수치(임계)는 발주처 협의 사항 — 기본값 보존.

## 남은 것 (코드 아님)
- **#4/#5 Postgres 실경로 검증**: advisory-lock·파티션 DDL은 dialect 가드로 비-PG에선 우회되어 로컬 lite에선 미실행. 배포 전 `make infra-up && alembic upgrade head` 후 PG 백엔드로 1회 검증 권장.
- **#11 신뢰도 임계 정책**: high_grade_review_min_reliability 운영값·검토자 자격 — 발주처 협의로 확정.
